on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/publish-v_rising.yml
      - v_rising/**
jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: ./v_rising
          push: true
          tags: ghcr.io/xynap/containers:v_rising
